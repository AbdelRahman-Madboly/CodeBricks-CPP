# 📘 Part 01: C++ Fundamentals

## 🎯 Objectives
* Build the mental model of how C++ handles data and memory.
* Master syntax, strict typing, and compilation basics.
* **Status:** [Completed]

## 📚 Topics Covered
1.  **Variables & DataTypes:** `int`, `float`, `double`, `char`, `bool`. Understanding size and memory.
2.  **Arithmetic & Priorities:** Operator precedence (`* / %` before `+ -`) and bracket logic.
3.  **Casting:** Explicit vs. Implicit casting (e.g., `(int)3.5` becomes `3`).
4.  **Scope:** The lifecycle of a variable. Local vs. Global scope.
5.  **Prefix vs Postfix:** The critical difference between `x++` and `++x`.

## 🧪 Key Exercises
* **Swap Two Numbers:** Swapping variables using a temp variable.
* **ASCII Value:** Printing the integer value of a `char`.
* **Circle Area:** Calculating area using `const` PI and proper type casting.

## 🧠 Interview Note
> "Remember that `x++` increments the value *after* the line executes, while `++x` increments it *before*. This is a common trick question."